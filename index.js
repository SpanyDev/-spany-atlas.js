const { isValidHexColor } = require("./src/isHexColor");
const { checkUpdate } = require("./src/checkUpdate");
const { isImageUrl } = require("./src/isImageUrl");
const { sonDepremler } = require("./src/deprem");
const { tdk } = require("./src/tdk");

module.exports = {
  isValidHexColor,
  checkUpdate,
  isImageUrl,
  sonDepremler,
  tdk,
};